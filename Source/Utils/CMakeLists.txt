set(SOURCE
        ${SOURCE}
        ${CMAKE_CURRENT_SOURCE_DIR}/Utils.cpp
        PARENT_SCOPE
        )

set(HEADER
        ${HEADER}
        ${CMAKE_CURRENT_SOURCE_DIR}/Utils.h
        PARENT_SCOPE
        )